@model CV.Management.Web.Models.NoticePeriodViewModel

<div class="row">

    @using (Html.BeginForm("NoticePeriod", "Cv", FormMethod.Post))
    {
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.AntiForgeryToken()

        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    @Html.LabelFor(model => Model.Period, new { @class = "control-label" })
                    @Html.TextBoxFor(model => Model.Period, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => Model.Period)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div class="form-group text-right">
                    <input type="submit" value="Save" class="btn btn-primary float-right" />
                </div>
            </div>
        </div>
    }

</div>
